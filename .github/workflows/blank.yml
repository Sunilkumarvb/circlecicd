name: ACCELQ GitHub Connect

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: ACCELQ GitHub Connect
        uses: accelQ-Inc/accelq-ci-github-actions@v1.4
        with:
          appURL: "https://poc.accelq.io/"
          userName: "sunilkumar.bhuvin@accelq.com"
          apiKey: "vpkDVzAX-JRqzk9K3aWxiDbpwa30S5V2U3205PjuKsI"
          tenantCode: "accelqtraining"
          jobId: "105615"

          # Optional Parameters
          # runParam: '{"username":"John","password":"Test123"}'
          # proxyHost: ""
          # proxyPort: ""
          # stepFailureThreshold: "0"
          # maxWaitTimeInMins: "15"
